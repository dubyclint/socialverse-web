// REMOVED: Vue 2 incompatible vue-toastification
// For Vue 3 Nuxt, use native notifications or Nuxt UI instead

export default defineNuxtPlugin((nuxtApp) => {
  // Toast notifications disabled - vue-toastification is Vue 2 only
  // Consider using: @nuxtui/ui, sonner, or native browser notifications
})
