// server/middleware/error-handler.ts
// SIMPLIFIED - No async operations

export default defineEventHandler((event) => {
  // Error handling is done in the plugin
  // This middleware just passes through
})
