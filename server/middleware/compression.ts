// server/middleware/compression.ts - SIMPLIFIED
// Compression disabled to prevent bundling issues

export default defineEventHandler((event) => {
  // Compression middleware disabled
  // Enable in production with proper configuration if needed
})
